#include "nexus/core/event.h"

// EventBus is header-only (template-based), but we keep this translation unit
// for potential future non-template utilities and to ensure the library links.

namespace nexus {
// Reserved for future event system utilities
} // namespace nexus
